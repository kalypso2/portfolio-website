import Projects from '../app/projects/projectsPage';  // Reuse previously created Projects page as a component

export default function ProjectsSection() {
    return (
        <section id="projects">
            <Projects />
        </section>
    );
}
